
import 'package:flutter/material.dart';

class ChatVoiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ChatVoice')),
      body: Center(child: Text('ChatVoiceScreen (placeholder)')),
    );
  }
}
