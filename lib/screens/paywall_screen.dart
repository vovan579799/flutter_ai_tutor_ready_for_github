
import 'package:flutter/material.dart';

class PaywallScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Paywall')),
      body: Center(child: Text('PaywallScreen (placeholder)')),
    );
  }
}



